rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write access for testing
    // WARNING: This is insecure - secure it before going to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}